const path = require('path');

module.export = {
    "config": path.resolve('./config', 'config.json'),
    "modules-path": path.resolve('./models'),
    "seeder-path": path.resolve('./seeders'),
    "migration-path": path.resolve('./migration')
};